# core/modes/ — Operating mode runners.
# Mode runners execute sequences of tools/actions when a mode is activated.
# They are NOT tools — they live outside the tool layer and call connectors directly.
